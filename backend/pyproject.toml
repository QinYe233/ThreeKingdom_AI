[project]
name = "sanguo-backend"
version = "1.0.0"
description = "AI三国演义后端服务"
license = {text = "CC-BY-NC-SA-4.0"}
requires-python = ">=3.11"

[tool.ruff]
line-length = 120
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B"]
